import React, { useState } from "react";
import { Box, Typography } from "@mui/material";

const AdditionalFeatures = () => {
  return (
    <Box sx={{ textAlign: "center" }}>
      <Typography>
        In order to access additional features, such as creating a sale listing
        and saving a sale listing, you need to create an account and login.
      </Typography>
    </Box>
  );
};

export default AdditionalFeatures;
